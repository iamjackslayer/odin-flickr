<%= form_for :user, url:{action: :create}, method: :post do |f| %>
ID: <%= f.text_field :id %>
<%= f.submit :submit %>
<% end %>
<% if @photos %>
	<% @photos.each do |photo_url| %>
	<%= image_tag(:photo_url) %>
<% end %>